{% extends 'layouts/default.njk' %} {% block body %}

<div class="wrapper">
  <nav id="main">
    <div class="header">
      <strong>Minha biblioteca</strong>
      <a href="/signout">Sair</a>
    </div>

    <div class="categories">
      <strong>CATEGORIAS</strong>
      <ul>
        {% for category in categories %}
        <li>
          <a href="/app/categories/{{category.id}}">
            {{category.title}}
            <span>{{category.Snippets.length}}</span>
          </a>
        </li>
        {% endfor %}
      </ul>
      <a href="#" data-modal-open="#new-category">NOVA CATEGORIA</a>
    </div>
  </nav>
  <div class="modal" id="new-category">
    <form method="POST" action="/app/categories/create" class="modal-content">
      <input type="text" name="title" placeholder="Título da categoria">
      <button type="submit">Salvar Categoria</button>
      <a href="#" data-modal-close="#new-category">Cancelar</a>
    </form>
  </div>
</div>

{% endblock%}
